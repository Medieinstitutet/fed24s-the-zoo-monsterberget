import React from 'react'
import { Navigation } from '../Navigation/Navigation'

export const Header = () => {
  return (
    <header>
        <div>Logga</div>
        <Navigation/>
    </header>
  )
}

export default Header
